
# Notify the frontend that we are starting the build

$d = (get-date).ToUniversalTime().toString('s')
Invoke-WebRequest ( $env:startPingUrl + '/' + $d )

cp \Users\rhub\Documents\run-test.ps1 .\run.ps1
cp \Users\rhub\Documents\slave-test.ps1 .\slave.ps1

.\run.ps1 -verbose $env:package $env:JOB_BASE_NAME $env:url $env:rversion $env:checkArgs $env:envVars
